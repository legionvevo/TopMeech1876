const express = require('express'); const path = require('path'); require('dotenv').config(); const app = express(); const PORT = process.env.PORT || 3000; // Serve static files app.use(express.static(path.join(__dirname, 'public'))); app.get('/api/lowest-number', (req, res) => { // Example logic for selecting lowest percentage number // Replace this with your actual Deriv bot logic const numbers = [ { number: 1, percentage: 12 }, { number: 2, percentage: 8 }, { number: 3, percentage: 15 } ]; const lowest = numbers.reduce((prev, curr) => prev.percentage < curr.percentage ? prev : curr); res.json(lowest); }); app.listen(PORT, () => { console.log(`TopMeech1876 bot running at http://localhost:${PORT}`); });
